import 'package:flutter/material.dart';

class NeoGrid extends StatelessWidget {
  final int columns;
  final List<Widget> children;

  const NeoGrid({
    required this.columns,
    required this.children,
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
